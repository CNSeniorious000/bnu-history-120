{
  "scripts": {
    "serve": "poetry run uvicorn app:app --reload --reload-include *.jinja2",
    "ssg": "uv sync && uv run ssg.py",
    "build": "unocss --minify",
    "dev": "unocss --watch"
  },
  "type": "module",
  "devDependencies": {
    "@iconify/json": "latest",
    "@netlify/edge-functions": "~3.0",
    "@unocss/cli": "~0.57",
    "@unocss/preset-icons": "~0.57",
    "@unocss/preset-uno": "~0.57"
  }
}
