{
  "scripts": {
    "serve": "poetry run uvicorn app:app --reload --reload-include *.jinja2",
    "ssg": "python3 install-deps.py && python3 ssg.py",
    "build": "unocss --minify",
    "dev": "unocss --watch"
  },
  "type": "module",
  "devDependencies": {
    "@iconify/json": "latest",
    "@netlify/edge-functions": "~2.2",
    "@unocss/cli": "~0.57",
    "@unocss/preset-icons": "~0.57",
    "@unocss/preset-uno": "~0.57"
  },
  "dependencies": {
    "openai": "~4.11"
  }
}
